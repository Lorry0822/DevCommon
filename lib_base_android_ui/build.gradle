apply plugin: 'com.android.library'
apply plugin: 'kotlin-android'

//apply plugin: 'com.novoda.bintray-release'
//
//publish {
//    userOrg = 'wangjie0822dev' // bintray账户下某个组织id
//    groupId = 'cn.wj.base' // maven仓库下库的包名，一般为模块包名
//    artifactId = 'dev-base-android-ui' //项目名称
//    publishVersion = '1.0.0' //版本号
//    desc = '' // 项目介绍，可以不写
//    website = '' // 项目主页，可以不写
//}

android {

    compileSdkVersion rootProject.ext.android.compileSdkVersion

    defaultConfig {
        minSdkVersion rootProject.ext.android.minSdkVersion
        targetSdkVersion rootProject.ext.android.targetSdkVersion
    }

    sourceSets {
        main { //           Java             Kotlin
            java.srcDirs = ['src/main/java', 'src/main/kotlin']
            jniLibs.srcDirs = ['libs']
        }
    }

    dataBinding { // DataBinding 开启
        enabled true
    }
}

dependencies {

    /* Kotlin 支持 */
    implementation "org.jetbrains.kotlin:kotlin-stdlib-jdk7:$kotlin_version"

    /* v7 支持*/
    implementation "androidx.appcompat:appcompat:${rootProject.ext.dependencies.androidx_appcompat_version}"

    /* 基本库 */
    api project(":lib_base_android")
//    releaseApi "cn.wj.base:dev-base-android:1.0.0"
}
